<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="MVVMCalculatorApp.Views.CalculatorsPage">
    <StackLayout Margin="20">
        <Entry Text="{Binding Num1, Mode=TwoWay}" />
        <Entry Text="{Binding Num2, Mode=TwoWay}" />
        

        <Button Text="ADD" Command="{Binding AddCommand}"></Button>
        <Button Text="SUBTRACT" Command="{Binding SubtractCommand}"></Button>
        <Button Text="MULTIPLY" Command="{Binding MultiplyCommand}"></Button>
        <Button Text="DIVIDE" Command="{Binding DivideCommand}"></Button>

        <Label Text="{Binding Result, Mode=TwoWay}"/>
    </StackLayout>
</ContentPage>
